# Git-document

`$ git init`


`$ git add <file-name>`

`$ git add -A`

`$ git diff HEAD`

`$ git diff --staged`

`$ git reset <file-name>`

`$ git checkout <file-name>`

`$ git checkout -- <file-name>`


`$ git commit -m "<commit description>"`
